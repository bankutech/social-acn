services:
  - type: web
    name: social-acn-backend
    plan: free
    env: node
    region: ohio
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: MONGO_URI
        value: mongodb+srv://sagnikmitra1302_db_user:ihEKnJ5DmzDzqrou@cluster0.vwuwxux.mongodb.net/acn_plus?appName=Cluster0
      - key: JWT_SECRET
        generateValue: true
    # Persistent disk for uploads (Requires Render Starter plan or above)
    # disk:
    #   name: uploads
    #   mountPath: /opt/render/project/src/backend/uploads
    #   sizeGB: 1
